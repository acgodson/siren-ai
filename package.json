{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "explorer",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "make compile && cd siren-web && pnpm i",
    "dev": "make compile &&  cd siren-web && npm run dev"
  },
  "type": "module",
  "workspaces": [
    "siren-web"
  ]
}
